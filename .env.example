# Copy to .env and fill in. Used for password login when no per-account
# cookies/<account>.auth.json is present and no --email/--password is passed.
DEEPSEEK_EMAIL=your-email@example.com
DEEPSEEK_PASSWORD=your-deepseek-password
